/**
 * jg-jpush-u 统一入口(UTS 插件规范:调用方从 utssdk/index.uts 导入)
 * 注意:UTS 不支持 export * from,必须显式导出函数
 */
// #ifdef APP-ANDROID
import * as impl from './app-android/index.uts'
export const init = impl.init
export const setDebug = impl.setDebug
export const getRegistrationId = impl.getRegistrationId
export const setEventCallBack = impl.setEventCallBack
// #endif
// #ifdef APP-IOS
// TODO: iOS 导出待补(与 Android 对齐后再开放;当前仅 Android 可用)
// #endif
