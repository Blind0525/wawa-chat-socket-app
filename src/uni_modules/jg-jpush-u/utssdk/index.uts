/**
 * jg-jpush-u 统一入口(UTS 插件必须有 utssdk/index.uts 才会被编译注册,
 * 否则运行时报 "jg-jpush-u not found")
 * 注意:UTS 不支持 export * from,必须显式 re-export
 */
// #ifdef APP-ANDROID
import { init, setDebug, getRegistrationId, setEventCallBack } from './app-android/index.uts'
export { init, setDebug, getRegistrationId, setEventCallBack }
// #endif
// #ifdef APP-IOS
// TODO: iOS 接入时补(与 app-ios/index.uts 的导出对齐)
// #endif
